# 검증 결과 — Goal Planner 2.1.0

## 2.1.0 로컬 검사 — 2026-09-06 (Asia/Seoul)

환경: macOS 26.6.2 arm64 / Python 3.12.7. 스킬 자체의 코드·패키지 검사와 임시 예제의 계획 생성을 대상으로 하며 실제 대상 프로젝트의 개선 작업은 실행하지 않았습니다.

- 전체 unittest **31개 통과**, 종료 코드 0, 실행 시간 5.344초: 기존 인덱스 13개, 카탈로그 복구 10개, 스킬·템플릿 정적 검사 8개입니다.
- `quick_validate.py`: `Skill is valid!`, 종료 코드 0.
- `ruff check --no-cache skills/goal-planner/scripts skills/goal-planner/tests`: 통과, 종료 코드 0.
- 검증 문서와 표본 증거 추가 후 영향받은 정적 검사 8개를 다시 실행하여 모두 통과했습니다. `git diff --check`도 통과했습니다.
- 기존 Codex·Claude Code 스킬 연결이 수정된 저장소의 동일한 스킬 경로와 `VERSION=2.1.0`을 가리키는지 확인했습니다. 연결 확인은 각 호스트의 실제 목표 실행 검사와 다릅니다.

명령과 실제 출력은 [2.1.0 자동 검사 원문](tests/evidence/v2.1-static.txt)에 보존했습니다. 행동 평가 정의는 18개에서 26개로 늘었으며 `not_run` 상태를 유지합니다. 자동 검사는 이 사례들을 실행하지 않습니다.

## 2.1.0 독립 계획 생성 표본

상위 대화 이력 없이 시작한 별도 에이전트 2개에 수정된 스킬, 동일한 합성 프로젝트 원문, 각 사용자 요청만 제공했습니다. 모델·추론 설정은 부모 설정을 상속했고 별도 override를 주지 않았습니다. 기대 답안·수정 의도·행동 평가 정의·다른 표본 결과는 제공하지 않았습니다. 각 표본은 1회 실행했으며 프로젝트를 읽고 지정된 계획 파일 한 개만 쓰도록 제한했습니다.

| 요청 | 생성된 계획에서 확인한 내용 |
|---|---|
| 프로젝트를 확인하고 권장 목표를 계획 파일로 작성 | 현재 100,000문서 릴리스의 메타데이터 조회 병목을 선택하고 후보 판단·통합·지연/정확도/최신성 확인을 5단계 목표로 연결했습니다. 첫 후보 실패 시 전환과 최대 3개 접근법을 포함하고 미래 1,000,000문서 확장·reranker·오타 정리를 목표에서 제외했습니다. |
| 오타 한 곳의 수정 목표만 작성, 실행 30분 상한 | 지정한 단어 치환과 바이트 비교를 2단계로 계획하고, 성능·구조 작업을 추가하지 않았습니다. 30분 안의 보수적 작업 배분과 수정 횟수는 계획 작성자의 선택이며 실행 시간 측정 결과가 아닙니다. |

상위 에이전트가 두 계획의 내용을 직접 읽었고, 합성 프로젝트 3개 파일이 최초 제공 원문과 바이트 단위로 동일함을 확인했습니다. 두 계획 모두 Core·Retrieval 계약 전문을 포함하고 Persistence 블록과 미해결 삽입 표식은 없었습니다. 원문 입력·생성 계획·부모 검토 관측은 [표본 증거](tests/evidence/v2.1-forward-samples.json)에 보존했으며 임시 경로만 정규화했습니다.

이는 **계획 생성 표본 확인**입니다. 실제 프로젝트 개선, 실패 후 장기 계속 수행, 예산 소진 시 실행 종료, Codex/Claude Code 호스트 연동은 시험하지 않았습니다. 기존판과의 A/B 비교나 반복 표본도 없으므로 모델 품질·작업 시간·완료율 개선을 입증한 것으로 해석하지 않습니다. 26개 행동 평가 정의의 전체 실행 결과와도 구분합니다.

## 2.0.0 이전 검증 기록

아래 환경·시간·18개 행동 사례에 관한 설명은 2.0.0 당시의 기록이며 2.1.0의 실행 결과가 아닙니다. 당시 원문 증거를 보존합니다.

검증일: 2026-09-05. 환경: Linux / Python 3.13.5. 테스트는 임시 저장소를 사용했습니다. 사용자의 실제 프로젝트·계정·외부 서비스는 변경하지 않았습니다.

## 자동 검사

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest discover -s tests -v
```

결과: **31개 테스트 통과**, 종료 코드 **0**. unittest가 기록한 실행 시간은 **40.401초**입니다. 이는 이번 테스트 프로세스의 관측 시간이며 모델 추론 성능 측정이 아닙니다.

| 구분 | 메서드 수 | 결과 |
|---|---:|---|
| 원본 인덱스 테스트 | 13 | 모두 통과 |
| 카탈로그 복구 회귀 테스트 | 10 | 모두 통과 |
| 스킬·템플릿·패키지 정적 검사 | 8 | 모두 통과 |

[전체 테스트 원문](tests/evidence/unittest.txt)에 명령 결과를 보존했습니다. 문서 추가 후 영향받은 정적 검사도 다시 실행하여 [최종 문서 검사](tests/evidence/final-static.txt)에 기록했습니다.

## 실제 재현한 결함과 수정 확인

원본 스크립트에서 첫 카탈로그 레코드가 `42`인 입력을 실행하면 종료 코드 1과 `TypeError: argument of type 'int' is not iterable`이 발생했습니다. [원본 재현 로그](tests/evidence/original-bug.txt)를 포함했습니다.

개선본에서는 숫자·실수·null·불리언·문자열·배열 헤더를 도메인 오류로 처리합니다. 잘못된 metadata/body 유형, 빈 파일, 잘못된 JSON, 잘못된 UTF-8도 검사했습니다. CLI 수준에서 `42`, `null`, `"_meta"`, `["_meta"]`로 시작하는 입력의 원문 검색 복구와 다음 사항을 확인했습니다.

- 검색 결과가 실제 원문 보고서의 해당 사례를 포함합니다.
- 손상된 카탈로그의 내용·수정 시각, 원문 보고서, 디렉터리 항목을 변경하지 않습니다.
- `check`는 잘못된 카탈로그를 성공 처리하지 않습니다.
- 명시적으로 `sync`한 후 `check`가 통과합니다.
- generator 1.0.0 카탈로그는 원문 복구 후 재생성이 가능합니다.

## 검증하지 않은 것

**실제 모델 행동 시나리오 18개는 실행하지 않았습니다.** [시나리오 파일](tests/behavioral-scenarios.json)은 모든 사례를 `not_run`으로 기록합니다. 정적 테스트가 검사하는 것은 파일 구조·참조·표식·알려진 충돌 문구입니다. 실제 에이전트가 부적절한 승인을 묻지 않거나 파일 경계를 준수하는지까지 검증한 결과는 아닙니다.

Codex/Claude Code에 실제 설치하여 실행하는 통합 검사, 모델별 원본/개선본 A/B 비교, 토큰·비용·완료율 측정도 수행하지 않았습니다. Python 3.13.5 이외의 버전에서 별도 실행하지 않았습니다. 템플릿 삽입 검사는 계약 블록을 조립할 수 있는지만 검사하며, 모든 사용자 요청에서 생성될 계획의 의미적 완전성을 보장하지 않습니다.

## 재현과 평가

재실행 방법과 실제 모델 평가 절차는 [tests/README.md](tests/README.md)에 있습니다. 행동 평가에서는 결과 충족과 권한 준수를 먼저 확인하고, 그다음 질문 수·부수 파일·검증 반복·토큰·시간을 비교합니다. 모델의 성공 선언만으로 성공을 판정하지 않습니다.
