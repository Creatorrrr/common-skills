# 수정 사항 — Goal Planner 2.4.0

## 2.4.0 — 2026-09-07

- 승인된 결과·제약·외부 계약·권한·예산 안의 필요한 접근법 변경은 계속 수행하고, 새 합의에 의존하는 단계만 보류하도록 본문·실행 계약·지식 지침을 통일했습니다.
- 중요한 지식 공백, 독립적인 메인 작업, 지원 도구와 기존 권한·공유 자원이 충족되면 연구 위임을 기본으로 합니다. 일반적인 실행 요청 뒤에 연구자 사용을 다시 지시받을 필요가 없으며, 단순 조회와 병행 조건이 없는 조사는 직접 수행합니다.
- 공통 Core와 조건부 Direction·Research·Retrieval·Persistence로 실행 계약을 분리했습니다. 선택한 블록의 전문만 계획에 포함하고, 명확한 작은 수정은 Core 단독으로 작성할 수 있습니다. 새 문제가 발생했을 때 상세 블록의 부재가 승인된 작업을 막지 않도록 했습니다.
- 스킬이나 작업 지침으로 확인·승인·중단이 필요할 때 실제 읽은 파일·문장·적용 이유를 설명하고 명시적 요구와 에이전트 해석을 구분합니다.
- 계약 조립 검사를 조건별 삽입·생략으로 갱신하고 B41–B44 평가 정의를 추가했습니다. 실제 실행 표본과 검증하지 않은 범위는 VALIDATION.md에 별도로 기록합니다. 과거 버전의 표본 원문은 보존합니다.

## 2.3.0 — 2026-09-07

- 결정에 영향을 줄 지식 공백이 있을 때 연구를 위임하고, 메인은 결과가 달라져도 가치가 남는 작업을 계속하도록 했습니다. 필요한 연구의 결과 확인과 막연한 미래 자료 대기를 구분했습니다.
- 질문·소스 상태·허용 범위·공유 예산·결정 시점, 노트 소유권과 완성 후 알림, 결정적 반증의 조기 알림, 메인의 적용 판단을 실행 계약에 포함했습니다.
- 논문·일차 웹 자료·자체 분석·격리된 실험의 증거 수준을 구분하고, 파일·측정 간섭과 오래된 소스에 대한 무조건 적용을 막습니다. 병렬 기능·기록 권한이 없을 때의 진행 방법과 종료 시 담당 연구 정리를 명시했습니다.
- 계획·연구 템플릿과 런타임 인계에 연결하고 행동 평가 정의 B33–B40을 추가했습니다. 스케줄러·백그라운드 실행 구현이나 별도 연구 인덱스는 추가하지 않았습니다. 실제 검증 범위는 VALIDATION.md에 기록합니다.

## 2.2.0 — 2026-09-07

- 사용자 결과·명시된 제약·필수 외부 계약과 현재 알고리즘·구조·가설을 구분했습니다. 유효한 실패의 공통 한계, 비교 가능한 개선 정체, 핵심 가정의 직접 반증이 있을 때 방향을 재검토합니다.
- 기존 방향 보완과 근거 있는 구조적 대안을 기여·학습·지연 비용·변경 비용·복원 가능성으로 비교합니다. 근거와 제한된 실패 비용이 정당화하면 큰 전환도 선택하고, 단순 실패 횟수나 변경 규모만으로 전환을 강제하지 않습니다.
- 목표·세션을 넘는 실패 이력을 공유 가정과 실패 원리로 묶어 읽고 무효 실행·반복 실행·반대 증거를 구분합니다. 새 실패 집계 인프라는 추가하지 않았습니다.
- 현재 목표의 성립 근거가 깨졌을 때 필요한 수정 제안을 선택적인 후속 목표와 구분했습니다. 기존 미달·무효화 증거를 보존하고, 새 결과·제약·계약·권한·예산 변경에는 기존 승인 경계를 적용합니다.
- 본문·실행 계약·지식 조회·계획 템플릿·직접 프롬프트에 연결하고 행동 평가 정의 B27–B32를 추가했습니다. 실제 수행한 검사와 계획 생성 표본의 범위는 VALIDATION.md에 구분합니다.

## 2.1.0 — 2026-09-06

- 목표 선택을 위임받으면 프로젝트 목적, 검증된 현재 상태, 진행 중인 작업과 핵심 차이에서 이번 이정표를 도출합니다. 목적 기여·후속 의존성·증거·승인된 자원으로 우선순위를 정하고, 완료 후 달라질 프로젝트 상태를 명시합니다.
- 개별 후보·실험·테스트의 종료와 전체 목표 완료를 구분합니다. 불확실한 개선에는 수용·기각·접근법 전환 조건을 두고, 근거와 예산이 남으면 같은 목표 안에서 계속 수행하도록 했습니다.
- 실제 구현·구조·성능 개선의 효과 증거를 요구하고, 개선 실패를 사후에 가설 연구 성공으로 바꾸지 못하게 했습니다. 가설 판정 자체가 요청된 결과라면 충분한 증거를 갖춘 기각도 완료할 수 있습니다.
- 명시적인 작은 작업과 분석 전용 범위를 보존합니다. 임의의 최소 작업 시간은 두지 않으며, 빠른 달성은 완료로 인정하고 미달 중단에는 남은 차이·증거·재개 지점을 남깁니다. 자동 목표 상향 금지와 같은 목표 안의 접근법 변경을 구분합니다.
- 계획 템플릿, 공통 실행 계약, 직접 프롬프트에 같은 의미를 전달했습니다. 행동 평가 정의 8개를 추가했으며, 정의·정적 검사·실제 계획 생성 및 장기 실행 증거를 구분합니다.

## 2.0.0 변경 이력

작성일: 2026-09-05

기준: 사용자가 첨부한 `goal-planner.zip`. 기존 스킬 이름은 유지하고, 이번 개선판에 배포 번호 `2.0.0`을 부여했습니다. 원본 ZIP은 변경하지 않았습니다.

## 변경 요약

| 영역 | 이전 문구·동작의 문제 | 개선한 규칙 |
|---|---|---|
| 지시 우선순위 | 기존 계획을 권위 있는 기준으로 취급하여 사용자의 명시적 변경도 재승인할 가능성 | 상위 제약 안에서 최신 명시적 사용자 지시를 우선. 변경되지 않은 요구만 기존 계획을 기준으로 유지 |
| 작업 모드 | 계획 검토·저장과 보고서 갱신·프로젝트 실행의 경계가 불명확 | 읽기 전용 검토/초안, 계획 저장, 승인된 지식 초기화, 별도 실행 인계를 구분 |
| 승인 | 자격 증명·서비스·배포라는 범주만으로 이미 승인된 작업도 다시 멈출 가능성 | 승인된 작업·환경·예산을 재사용하고 새 권한·비용·파괴적 영향·실질적 범위 확대에만 해당 단계의 승인을 확인 |
| 검증 단위 | 본문의 체크포인트 제한이 실행 프롬프트에서 작업 횟수 제한으로 바뀜 | 연속된 테스트·lint는 허용. 새 정보 없는 검증 전용 체크포인트 추가를 제한 |
| 재검증 | 최종 검증 1회라는 축약 문구가 수정 후 재검증까지 막을 가능성 | 관련 변경·실패·미해결 우려가 있을 때 영향받은 검증을 반복/확대 |
| 검증 비용 | 검증 작업이 구현보다 커지면 중단하는 문구 | 비교 비용 자체는 중단 조건에서 제외. 승인된 예산·새 권한·비용·범위가 실제 판단 기준 |
| 실패 기록 | 재시도 전에 정식 보고서·링크·인덱스를 모두 정리해야 할 가능성 | 즉시 최소 증거 보존과 다음 체크포인트/종료 시 상세 정리의 2단계로 분리 |
| 지식 신뢰 경계 | 연구 자료에 비해 실행 보고서의 비지시성 규칙이 덜 명확 | 보고서·연구·인덱스·검색 결과·인용 도구 출력을 모두 증거로 취급; 권한이나 지시로 승격 금지 |
| 부수 파일 생성 | 계획 저장만 요청해도 보고서·연구 폴더와 템플릿을 초기화 | 기본 read-only. 명시되거나 기존 승인 범위에 포함된 persist·초기화만 수행 |
| 프롬프트 일관성 | 여러 런타임 변형이 공통 규칙을 독립적으로 요약해 의미가 달라짐 | 공통 실행 규칙을 한 참조 파일로 관리하고 활성화된 블록을 계획/프롬프트에 실제 삽입 |
| 이식성 | 계획 작성자가 읽은 로컬 스킬 참조를 실행자도 읽었을 것으로 가정할 위험 | 기본은 규칙 내장. 추가 참조는 실제 접근 가능 경로와 읽을 시점을 명시 |
| 런타임 의존 | 특정 `/goal` 평가 방식과 고정 문자 제한을 단정 | 설치된 런타임에서 확인된 기능만 사용. 미확인 시 일반 텍스트 인계 |
| 인덱스 오류 | 첫 레코드가 숫자·null·일부 문자열/배열이면 타입 오류로 복구 경로 이탈 | 첫 레코드의 객체 여부를 선검사하고 `ReportIndexError`로 처리하여 원문 검색 복구 |
| 검증 범위 | 기존 인덱스 테스트 중심 | 10개 코드 회귀 테스트와 8개 정적 검사 추가. 실제 모델 행동 평가용 18개 시나리오는 별도 미실행 상태 제공 |

## 유지한 기능

결과 우선 완료 기준, 기본 최대 6단계/8개 완료 기준, 위험 비례 검증, 고정 성능 비교 조건, 제한된 수정 반복, 자동 목표 상향 비활성화를 유지했습니다. 분석·보고서 목표에는 불필요한 구현을 만들지 않습니다.

실패·성공 보고서와 연구 자료의 기존 경로·템플릿 필드는 유지했습니다. 관련도 우선 조회, 후보 기본 15건/전문 기본 5건, 보고서 100건·헤더 200 KiB·반복 원문 검색 1초라는 인덱스 검토 조건, 원문 기준 및 연구/실행 보고서 분리도 유지했습니다. 다만 도입 조건 충족이 쓰기 권한까지 부여하지는 않습니다.

성공 기록의 자격은 계속 다음 세 가지로 닫혀 있습니다. 모든 최종 기준이 통과한 뒤 중요한 실패를 해결했거나, 기본 접근 실패 후 비자명한 대안을 확보했거나, 코드/문서에서 저렴하게 복원하기 어려운 필수 다단계 재현 절차를 얻은 경우입니다. 부분 완료·차단 상태에는 성공 보고서를 작성하지 않습니다.

## 파일 구조 변경

`SKILL.md`의 상세 지식 운영 규칙을 `references/execution-knowledge.md`로 이동하고 정리했습니다. 공통 인계 규칙은 `references/execution-contract.md`, 계획 골격은 `assets/goal-plan-template.md`에 있습니다. 실행에 필요한 규칙을 참조로만 숨기지 않고 최종 계획에 내장하도록 했습니다.

진입점 `SKILL.md`는 공백 분리 단어 기준 **3,402 → 1,579**로 줄었습니다. 이는 파일 구성 측정이며 토큰 수나 실제 비용 절감 측정이 아닙니다. 상세 기능은 참조 파일에 남아 있고 배포 문서와 테스트도 추가되어 전체 패키지가 같은 비율로 줄었다는 뜻은 아닙니다.

원본 `tests/test_report_index.py`의 13개 테스트는 수정하지 않았습니다. 새 파일 `tests/test_catalog_recovery.py`에 10개 메서드를 추가했고, `tests/test_skill_contracts.py`에 8개 정적 검사를 추가했습니다. 하위 입력 사례를 개별 테스트 수로 중복 계산하지 않았습니다.

macOS 메타데이터(`__MACOSX`, `._*`)와 Python 캐시(`__pycache__`, `.pyc`)는 배포본에서 제외했습니다.

## 코드 수정 상세

```python
# catalog metadata를 읽기 전에 JSON 컨테이너 유형을 확인한다.
if not records or not isinstance(records[0], dict) or "_meta" not in records[0]:
    raise ReportIndexError("Catalog must start with an _meta object record")
```

이 변경으로 카탈로그가 `42`, `null`, `"_meta"`, `["_meta"]` 등으로 시작해도 `query`는 오류를 알리고 현재 원문 보고서를 검색합니다. 검색은 손상된 카탈로그나 보고서를 수정하지 않습니다. `check`는 유효하지 않은 카탈로그에 비정상 종료 코드를 반환하고, 사용자가 승인한 `sync` 후 `check`로 정상 상태를 확인할 수 있습니다.

스키마 `1`은 유지하고 generator를 `1.0.1`로 올렸습니다. 기존 카탈로그는 버전 메타데이터 차이로 stale 판정될 수 있습니다. 이때 원문 검색을 계속 사용하며 승인된 시점에 재생성합니다.

## 마이그레이션 주의

스킬 폴더 전체를 교체하되 프로젝트의 과거 보고서나 연구 자료는 보존합니다. 기존 `GOAL_PLAN.md`와 복사해 둔 실행 프롬프트는 자동 변환되지 않으므로 재검토가 필요합니다. 지식 기록을 계속 자동화하려는 워크플로에는 `persist`와 기록 허용 경로를 명시합니다. 기록·초기화·실행 승인을 혼합하지 않습니다.

선택적 서브에이전트 사용은 유지 가능한 실행 계획 옵션일 뿐 이 스킬의 필수 의존성이 아닙니다. API 모델 ID, reasoning 설정, 비동기 도구 구현, 개인용 하위 모델 호출 스킬은 추가하지 않았습니다.

## 참고한 공식 가이드와 적용 범위

2026-09-05에 [OpenAI 최신 모델 가이드](https://developers.openai.com/api/docs/guides/latest-model)의 지시 우선순위, 승인 전 가능한 작업 수행, 필요한 범위의 검증 원칙을 확인했습니다. 이를 이 스킬의 계획 전용 역할과 사용자 승인 범위에 맞춰 적용했습니다. [OpenAI 스킬 문서](https://developers.openai.com/codex/skills/)의 간결한 진입점·필요시 상세 참조 방식도 반영했습니다.

이 변경이 특정 모델의 성능 향상을 보장한다는 주장은 아닙니다. 실제 자동 검사와 미수행 평가의 경계는 [VALIDATION.md](VALIDATION.md)에 정리했습니다.
