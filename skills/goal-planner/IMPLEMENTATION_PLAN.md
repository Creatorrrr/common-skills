# goal-planner 3.2.0 — implementation plan (frozen before implementation)

Scope: improve the supplied 3.1.0 skill, not execute a target project. Preserve existing default read-only/planning operation, seven portable contract blocks, finite/program distinction, existing report-index and preflight APIs, outcome evidence, delegation and budget boundaries.

## Intended outcome
Provide portable evaluator-first evolutionary research planning with an optional deterministic, read-only candidate portfolio inspector and handoff compiler. Do not claim a full autonomous research engine, new model discovery, or empirical prompt/model superiority.

## Changes
1. Add conditional evolutionary design reference: domain contract, evaluator calibration/negative controls, immutable evaluation boundary, progressive cost gates, scope/scale transfer, diversity and explicit plateau decision, compact falsifiable lessons.
2. Incorporate minimal executable semantics into existing Experiment, Direction, Program blocks (no eighth mandatory role/block; simple planning remains light).
3. Implement stdlib-only research_portfolio.py: schema validation, exact comparison cohorts, ordered stage gates, constraints, comparable Pareto front, family-aware references, duplicate/conflict handling, confirmation binding, separate budget units. All outputs are suggestions based on supplied declarations; no code execution, mutation, adoption, spending, or fake external verification.
4. Implement build_handoff.py: seven canonical named blocks, chosen blocks only, no duplicates/unresolved markers, output cap fails without truncation. Report exact bytes/characters, not guessed token savings.
5. Preserve 99 existing tests, updating version assertion only; add positive/negative/unit/CLI tests and explicit not-run behavioral cases. Use a small self-contained fixture to exercise real helper behavior.
6. Document new usage, in-place migration, research-to-change traceability, current official model-guide snapshot and honest evaluation limits. No specific project, provider, model slug, reasoning setting or install automation in the skill core.

## Pre-run acceptance
- All 99 prior unit/contract tests still pass; only version assertion may change for 3.2.0.
- Malformed, non-finite, boolean numeric, duplicate identifiers, mixed cohorts, gate failures, confirmation hash mismatch cannot lead to confirmed suggestions.
- Reads do not mutate input/source and launch no child/model processes.
- Bounded handoff refuses silent truncation and never adds persistence by default.
- Existing source/history preserved; artifact manifest verifies packaged files; no caches/secrets/macOS metadata.
- New tests measure implementation and static contracts, not LLM behavior. End-to-end scientific performance, cost/token savings and model A/B remain unmeasured.

## Amendments
Append justified corrections here; do not change the acceptance criteria to fit observed failures.
